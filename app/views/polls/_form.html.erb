<%= form_for(@poll) do |f| %>
  <% if @poll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@poll.errors.count, "error") %> prohibited this poll from being saved:</h2>

      <ul>
      <% @poll.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :poll_no %><br>
    <%= f.text_field :poll_no %>
  </div>
  <div class="field">
    <%= f.label :poll_name %><br>
    <%= f.text_field :poll_name %>
  </div>
  <div class="field">
    <%= f.label :poll_description %><br>
    <%= f.text_field :poll_description %>
  </div>
  <div class="field">
    <%= f.label :poll_start_date %><br>
    <%= f.text_field :poll_start_date %>
  </div>
  <div class="field">
    <%= f.label :poll_finish_date %><br>
    <%= f.datetime_select :poll_finish_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
